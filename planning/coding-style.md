CODING STYLE:

Python:

funciton names must be snake-cased;

variable names must be camel-cased;

class names must be pascal-cased.


value names must not be abbreviated.


every variable must have a type hint in their first instance unless it equals another variable.

if multiple lines are stored in brackets, the code structure must be as follows:

```
def function(
    valueOne,
    valueTwo = otherValue,
    soOn
):
    # function code here...
```



General:

code like a never-nester unless necessary.

explain why a function/class does what it does.


File Stricture:

Module Imports

Variables

Functions

Classes

Execution
