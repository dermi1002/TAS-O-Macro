INPUT SCRIPT:

supports x-input only

syntax:

every line counts as a frame

not all inputs are present in line, only those pressed

if there isn't a way to end reading a file without this method, script ends with the 'end' keyword

input definition:

lx-XXXXX = left stick X position

ly-XXXXX = left stick Y position

rx-XXXXX, ry-XXXXX, same thing from left stick applied to right stick

lt-XXX, rt-XXX = left and right triggers' values

d-up, d-down, d-left, d-right = dpad directions

a, b, x, y = face buttons

lb, rb = left and right bumpers

start = start

remind me what the button is to the left of the xbox button?

INPUT SCRIPT INTERPRETER:

no plans as of yet
